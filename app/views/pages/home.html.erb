<body style="height: 100" %>
  <div class="signup" style="background-image: url('https://media.istockphoto.com/photos/healthy-fresh-rainbow-colored-fruits-and-vegetables-background-picture-id1208790371?k=20&m=1208790371&s=612x612&w=0&h=6BngNrl8TColGkvSGJUKFKIM5bv31Nc8MvQhmmC2LlM=');">
    <div class="textbox">
      <p class="hometext" > <strong>Streamline the group shop</strong>
      </p>
    </div>
    <div class="buttonbox">
      <div>
      <%= link_to "New Household", new_household_path, class: "btn btn-main-green" %>
      </div>
      <div>
        <% if user_signed_in? %>
          <%= link_to "#{current_user.household.name}'s Household", household_path(current_user), class: "btn btn-main-green" %>
        <% else %>
          <%= link_to "Sign Up", user_session_path, class: "btn btn-main-blue" %>
        <%end%>

      <div>
        <% if user_signed_in? %>
          <%= link_to "#{current_user.name.capitalize}'s dashboard", household_path(current_user), class: "btn btn-main-blue" %>
        <% else %>
          <%= link_to "Log in", user_session_path, class: "btn btn-main-blue" %>
        <%end%>
      </div>
    </div>
  </div>
</body>
